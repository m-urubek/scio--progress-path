# Progress Path - Environment Variables
# ======================================
# Copy this file to .env and fill in your values.
# These variables are used by Docker Compose for local development.
#
# IMPORTANT: Never commit .env to version control!
#
# NAMING CONVENTION NOTE:
# -----------------------
# Variables use double-underscore (__) format for ASP.NET Core configuration binding.
# For example, "LLM__Provider" maps to "LLM:Provider" in the configuration hierarchy.
#
# Alternative formats (for reference, PRD uses underscores):
#   PRD Format         ->  .NET Config Format (used here)
#   LLM_PROVIDER       ->  LLM__Provider
#   LLM_MODEL          ->  LLM__Model
#   LLM_API_KEY        ->  LLM__ApiKey
#   GOOGLE_CLIENT_ID   ->  GoogleAuth__ClientId
#   GOOGLE_CLIENT_SECRET -> GoogleAuth__ClientSecret
#   APPLICATION_BASE_URL -> Application__BaseUrl

# -----------------------------------------------------------------------------
# SQL Server Configuration
# -----------------------------------------------------------------------------
# SA_PASSWORD must meet SQL Server complexity requirements:
# - Minimum 8 characters
# - Must contain at least 3 of: uppercase, lowercase, number, special character
# Example: YourStrong!Passw0rd
SA_PASSWORD=

# -----------------------------------------------------------------------------
# LLM Provider Configuration
# -----------------------------------------------------------------------------
# Provider: The LLM provider to use (e.g., "openai", "anthropic", "azure")
# Model: The model name (e.g., "gpt-4", "gpt-4o", "claude-3-sonnet")
# ApiKey: Your API key for the LLM provider
#
# Alternative variable names (PRD format): LLM_PROVIDER, LLM_MODEL, LLM_API_KEY
LLM__Provider=openai
LLM__Model=gpt-4o
LLM__ApiKey=

# -----------------------------------------------------------------------------
# Google OAuth Configuration
# -----------------------------------------------------------------------------
# Create OAuth 2.0 credentials at: https://console.cloud.google.com/apis/credentials
# Set authorized redirect URI to: http://localhost:5000/signin-google
#
# Alternative variable names (PRD format): GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET
GoogleAuth__ClientId=
GoogleAuth__ClientSecret=

# -----------------------------------------------------------------------------
# Dev Authentication (Test Login)
# -----------------------------------------------------------------------------
# Set to "true" to show a "Dev Login" button on the login page that bypasses
# Google OAuth and logs in as a fictional test teacher.
# When "false" or absent, the button is not rendered and the endpoint returns 404.
DevAuth__Enabled=true

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
# Base URL for generating QR codes and join links
# For local development, use http://localhost:5000
# For production, use your deployed URL (e.g., https://myapp.azurewebsites.net)
#
# Alternative variable name (PRD format): APPLICATION_BASE_URL
Application__BaseUrl=http://localhost:5000
